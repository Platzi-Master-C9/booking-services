const {
  getPlaces,
  updatePlace,
  deletePlace,
  getPlace,
  geocoding,
  reverseGeocoding,
} = require('./places');

module.exports = {
  getPlaces,
  deletePlace,
  updatePlace,
  getPlace,
  geocoding,
  reverseGeocoding,
};
