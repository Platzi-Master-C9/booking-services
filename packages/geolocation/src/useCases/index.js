const {
  getPlaces,
  updatePlace,
  deletePlace,
  getPlace,
  geocoding,
  reverseGeocoding,
  createPlace,
} = require('./places');

module.exports = {
  getPlaces,
  deletePlace,
  updatePlace,
  getPlace,
  geocoding,
  reverseGeocoding,
  createPlace,
};
