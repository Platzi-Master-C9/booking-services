const welcome = require('./utils/welcomPackage');

// Services
// const { chatServices } = require('./useCases');

// // Domains
// const { ChatModel } = require('./domains');

module.exports = {
  // createChat: chatServices.createChat(ChatModel),
  messagesWelcome: welcome,
};
